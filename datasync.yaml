targets:
  - kind: mysql
    config:
      host: localhost
      port: 3300
      user: root
      password: root
      database: sample
  # - kind: file
  #   config:
  #     path: "./cmd"
  # - kind: file
  #   config:
  #     path: "./.vscode"
  - kind: file
    config:
      path: "./LICENSE"

storage:
  kind: gcs
  config:
    bucket: datasync000001
    dir: "test01/test02"
