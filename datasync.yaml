targets:
  - kind: mysql
    config:
      host: localhost
      port: 3306
      user: root
      password: root
      database: sample
  # - kind: file
  #   config:
  #     path: "./cmd"
  - kind: file
    config:
      path: "./dump/dump_mysql"

storage:
  kind: gcs
  config:
    bucket: datasync000001
    dir: "test01/test02"
